# 미점등 신호등 오탐 문제와 TODO

## 문제점

- 모든 등이 꺼진 신호등을 점등된 신호등으로 탐지하는 오탐이 발생한다.
- 검증 영상에서 `sign_yellow 0.88`, `sign_green 0.85`로 탐지된 사례를 확인했다.
- 사용자 관찰상 밝은 환경에서 노란불의 점등/미점등 구분이 특히 어렵고, 어두운 환경에서는 상대적으로 잘 구분한다.
- 커버 색상·반사광·신호등 외형을 점등 판단의 단서로 사용했을 가능성이 있으나 원인은 추가 검증이 필요하다.
- 높은 confidence에서도 오탐이 발생하므로 임계값 조절만으로 해결되는지 확인이 필요하다.

## 데이터 처리 시 확인할 사항

- 전체 원본 이미지와 라벨은 사용자가 보관한 `raw` 폴더를 기준으로 처리한다. 실제 경로는 작업 전에 확인한다.
- makesense.ai에서 박스를 지정하지 않은 이미지는 TXT가 생성되지 않을 수 있다.
- 현재 `scripts/split_dataset.py`는 대응 TXT가 없는 이미지를 건너뛴다. 따라서 원본에 이미지가 있어도 분할된 학습 데이터에서 제외될 수 있다.
- 설치된 Ultralytics는 누락되거나 빈 라벨을 배경 예제로 처리하지만, 현재 분할 스크립트를 통과하려면 빈 TXT가 필요하다.
- 빈 라벨은 탐지 대상이 없는 이미지에만 사용한다. 함께 보이는 `knob`이나 점등된 신호등 등 기존 대상은 반드시 라벨링한다.

## TODO

- [ ] 원본 `raw` 경로와 이미지/TXT 매칭 상태를 확인하고 누락 개수를 집계한다.
- [ ] 미점등 신호등 데이터를 밝기·거리·각도·반사 조건별로 확보한다. 화면 가장자리에 일부만 보이는 사례도 포함한다.
- [ ] 같은 카메라 위치와 조명에서 점등/미점등을 번갈아 촬영해 비교 가능한 예제를 확보한다.
- [ ] 소등 상태 자체의 탐지가 필요한지 정한다. 필요하면 `sign_off` 클래스를 추가하고, 점등 신호만 필요하면 미점등 이미지를 배경 예제로 사용한다.
- [ ] 배경 예제로 사용할 이미지에만 동일한 이름의 빈 TXT를 생성한다. 기존 라벨을 덮어쓰지 않고, 실제 라벨링 누락과 구분한다.
- [ ] `sign_off`를 추가하는 경우 기존 클래스 ID를 유지하고 클래스 정의와 라벨을 함께 갱신한다.
- [ ] 원본 기준으로 학습/검증/테스트 데이터를 구성하고, 미점등 이미지가 학습 세트에 실제 포함됐는지 확인한다.
- [ ] 같은 영상의 인접 프레임이 학습과 검증에 섞이지 않도록 영상 또는 촬영 세션 단위로 분리한다. 기존 모델이 학습한 이미지도 최종 독립 평가에서 제외한다.
- [ ] 기존 점등 데이터와 미점등 데이터를 함께 사용해 추가 학습하고, 가중치와 평가 결과를 별도 실행 폴더에 기록한다.
- [ ] 별도로 남긴 영상으로 밝은 환경/어두운 환경의 소등 오탐과 실제 점등 미탐을 기존 모델과 비교한다.
- [ ] 밝은 환경에서 노출을 조절했을 때 점등/미점등 차이가 개선되는지 확인한다.

## 현재 작업 범위

이번 작업은 문제와 후속 작업의 문서화만 수행한다. 데이터, 라벨, 분할, 클래스 설정 및 학습 코드는 변경하지 않는다.
